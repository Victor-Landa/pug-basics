doctype html
html
  head
    link(rel="stylesheet" href="style.css")
    style.
      p {
        color: blue;
        text-decoration: underline;
      }
  body
    p(style="font-size: 28px; text-align: center;") Testing css files in pug
    
    - const pStyles = {"text-transform": "uppercase", "font-size": "20px", "text-align": "center"}
    p(style=pStyles) Second paragraph, styles added with js