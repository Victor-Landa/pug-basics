doctype html
html
  head
    title Conditionals in Pug
  body
    h2 Web Application
    - let user = { name: "Victor", loggedIn: false, lastLogin: 6 }
    if user.loggedIn
      p
        | Welcome back, 
        strong #{ user.name }
    else if user.lastLogin < 12
      p Your last login was #{ user.lastLogin } minutes ago
      p
        a(href="/login") Log In Again
    else
      a(href="/login") Log In